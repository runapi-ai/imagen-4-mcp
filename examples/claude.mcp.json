{
  "mcpServers": {
    "imagen-4": {
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/imagen-4-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
